/**
 * The <i>Configuration</i> package holds all classes and object connected to the app properties and configuration,
 * with the exception being security configuration which are located in {@link pwr.pracainz.security}.
 * Additionally, the package hold all custom JSON serializers and Deserializers.
 *
 * @since 1.0
 * @author Kacper Z.
 * @version 1
 */
package pwr.pracainz.configuration;