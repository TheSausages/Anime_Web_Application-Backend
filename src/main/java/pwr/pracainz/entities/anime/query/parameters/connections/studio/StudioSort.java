package pwr.pracainz.entities.anime.query.parameters.connections.studio;

public enum StudioSort {
    ID,
    ID_DESC,
    NAME,
    NAME_DESC,
    SEARCH_MATCH,
    FAVOURITES,
    FAVOURITES_DESC
}
